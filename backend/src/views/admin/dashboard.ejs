<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#f8fafc',
                            500: '#0f172a',
                            600: '#0f172a',
                            700: '#020617'
                        },
                        ruby: '#b91c1c',
                        emerald: '#047857',
                        golden: '#facc15'
                    }
                }
            }
        }
    </script>
    
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/admin/assets/css/admin.css">
    
    <style>
        .dashboard-gradient {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
        }
        .stats-card {
            transition: all 0.3s ease;
            background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
        }
        .stats-card:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }
        
        /* Top performers card overflow fix */
        .top-performer-card {
            display: flex !important;
            align-items: center !important;
            justify-content: space-between !important;
            width: 100% !important;
            overflow: hidden !important;
        }
        
        .top-performer-content {
            flex: 1 !important;
            min-width: 0 !important;
            margin-right: 1rem !important;
        }
        
        .top-performer-views {
            flex-shrink: 0 !important;
            white-space: nowrap !important;
        }
        
        /* Recent cards hover effects */
        .recent-gemstone-card {
            position: relative;
            overflow: hidden;
        }
        
        .recent-gemstone-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }
        
        .recent-gemstone-card:hover::before {
            left: 100%;
        }
        .counter-animation {
            animation: counter-up 1.5s ease-out;
        }
        @keyframes counter-up {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .floating-animation {
            animation: floating 3s ease-in-out infinite;
        }
        @keyframes floating {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
        .gradient-text {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        /* Progress bar animation */
        .progress-bar {
            width: 0%;
            transition: width 1s ease-out;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-50 to-blue-50 min-h-screen">
    <!-- Mobile menu overlay -->
    <div id="mobile-menu-overlay" class="fixed inset-0 bg-gray-600 bg-opacity-75 z-20 lg:hidden hidden"></div>
    
    <!-- Sidebar -->
    <div id="sidebar" class="fixed inset-y-0 left-0 z-30 w-64 bg-primary-500 text-white transform -translate-x-full lg:translate-x-0 transition duration-300 ease-in-out">
        <div class="flex items-center justify-center h-16 bg-primary-700">
            <div class="flex items-center space-x-2">
                <img src="/admin/assets/img/kohinoor-logo.png" alt="Kohinoor Logo" class="w-8 h-8 object-contain">
                <h1 class="text-xl font-bold">Kohinoor Admin</h1>
            </div>
            <button id="close-sidebar" class="absolute top-4 right-4 lg:hidden">
                <i data-lucide="x" class="w-6 h-6"></i>
            </button>
        </div>
        
        <nav class="mt-5 px-2">
            <a href="/admin/dashboard" class="group flex items-center px-2 py-2 text-base font-medium rounded-md hover:bg-primary-600 bg-primary-600">
                <i data-lucide="layout-dashboard" class="mr-4 h-6 w-6"></i>
                Dashboard
            </a>
            
            <a href="/admin/gemstones" class="group flex items-center px-2 py-2 mt-1 text-base font-medium rounded-md hover:bg-primary-600 transition duration-200">
                <i data-lucide="gem" class="mr-4 h-6 w-6"></i>
                Gemstones
            </a>
            
            <a href="/admin/business" class="group flex items-center px-2 py-2 mt-1 text-base font-medium rounded-md hover:bg-primary-600 transition duration-200">
                <i data-lucide="building" class="mr-4 h-6 w-6"></i>
                Business Info
            </a>
            
            <div class="mt-8 pt-4 border-t border-primary-600">
                <a href="/admin/logout" class="group flex items-center px-2 py-2 text-base font-medium rounded-md hover:bg-red-600 transition duration-200">
                    <i data-lucide="log-out" class="mr-4 h-6 w-6"></i>
                    Logout
                </a>
            </div>
        </nav>
    </div>

    <!-- Main content -->
    <div class="lg:pl-64 flex flex-col flex-1">
        <!-- Top bar -->
        <div class="sticky top-0 z-10 lg:hidden bg-white shadow-sm border-b border-gray-200">
            <div class="flex items-center justify-between h-16 px-4">
                <button id="mobile-menu-button" class="text-gray-500 hover:text-gray-900">
                    <i data-lucide="menu" class="w-6 h-6"></i>
                </button>
                <div class="flex items-center space-x-2">
                    <img src="/admin/assets/img/kohinoor-logo.png" alt="Kohinoor Logo" class="w-6 h-6 object-contain">
                    <h1 class="text-lg font-semibold text-gray-900">Admin Dashboard</h1>
                </div>
                <div class="flex items-center">
                    <span class="text-sm text-gray-500"><%= user.name %></span>
                </div>
            </div>
        </div>

        <!-- Page content -->
        <div class="flex-1 flex flex-col">
            <!-- Stunning Header -->
            <header class="dashboard-gradient shadow-2xl hidden lg:block relative overflow-hidden">
                <div class="absolute inset-0 bg-black bg-opacity-10"></div>
                <div class="relative px-6 py-12">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center space-x-6">
                            <div class="w-20 h-20 bg-white bg-opacity-20 rounded-full flex items-center justify-center floating-animation">
                                <i data-lucide="crown" class="w-10 h-10 text-white"></i>
                            </div>
                            <div>
                                <h1 class="text-5xl font-bold text-white mb-3">Welcome to Your Kingdom</h1>
                                <p class="text-blue-100 text-xl">Manage your precious gemstone empire with royal elegance</p>
                            </div>
                        </div>
                        <div class="flex items-center space-x-6">
                            <div class="text-right">
                                <div class="text-white text-2xl font-bold">👑 <%= user.name %></div>
                                <div class="flex items-center mt-2">
                                    <span class="px-4 py-2 bg-white bg-opacity-20 text-white text-sm font-bold rounded-full border border-white border-opacity-30">
                                        ✨ <%= user.role.toUpperCase() %>
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </header>

            <!-- Main content area -->
            <main class="flex-1 p-4 sm:p-6 lg:p-8 w-full max-w-none">
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="mb-6 p-4 bg-red-50 border-l-4 border-red-400 rounded-r-lg shadow-sm">
                        <div class="flex items-center">
                            <i data-lucide="alert-circle" class="w-5 h-5 mr-3 text-red-500"></i>
                            <p class="text-red-800 font-medium"><%= error %></p>
                        </div>
                    </div>
                <% } %>
                
                <% if (typeof success !== 'undefined' && success) { %>
                    <div class="mb-6 p-4 bg-green-50 border-l-4 border-green-400 rounded-r-lg shadow-sm">
                        <div class="flex items-center">
                            <i data-lucide="check-circle" class="w-5 h-5 mr-3 text-green-500"></i>
                            <p class="text-green-800 font-medium"><%= success %></p>
                        </div>
                    </div>
                <% } %>

                <!-- Stunning Stats Cards -->
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
                    <!-- Total Gemstones -->
                    <div class="stats-card rounded-2xl shadow-xl p-6 border border-blue-200">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-blue-600 text-sm font-semibold uppercase tracking-wider mb-1">Total Collection</p>
                                <p class="text-4xl font-bold text-blue-700 counter-animation"><%= stats.totalGemstones %></p>
                                <p class="text-blue-500 text-sm mt-1">💎 Precious Stones</p>
                            </div>
                            <div class="w-16 h-16 bg-gradient-to-br from-blue-400 to-blue-600 rounded-full flex items-center justify-center shadow-lg">
                                <i data-lucide="gem" class="w-8 h-8 text-white"></i>
                            </div>
                        </div>
                    </div>

                    <!-- Trending Gemstones -->
                    <div class="stats-card rounded-2xl shadow-xl p-6 border border-orange-200">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-orange-600 text-sm font-semibold uppercase tracking-wider mb-1">Trending Now</p>
                                <p class="text-4xl font-bold text-orange-700 counter-animation"><%= stats.trendingGemstones %></p>
                                <p class="text-orange-500 text-sm mt-1">🔥 Hot Picks</p>
                            </div>
                            <div class="w-16 h-16 bg-gradient-to-br from-orange-400 to-orange-600 rounded-full flex items-center justify-center shadow-lg floating-animation">
                                <i data-lucide="trending-up" class="w-8 h-8 text-white"></i>
                            </div>
                        </div>
                    </div>

                    <!-- Featured Gemstones -->
                    <div class="stats-card rounded-2xl shadow-xl p-6 border border-emerald-200">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-emerald-600 text-sm font-semibold uppercase tracking-wider mb-1">Featured</p>
                                <p class="text-4xl font-bold text-emerald-700 counter-animation"><%= stats.featuredGemstones %></p>
                                <p class="text-emerald-500 text-sm mt-1">⭐ Highlights</p>
                            </div>
                            <div class="w-16 h-16 bg-gradient-to-br from-emerald-400 to-emerald-600 rounded-full flex items-center justify-center shadow-lg">
                                <i data-lucide="star" class="w-8 h-8 text-white"></i>
                            </div>
                        </div>
                    </div>

                    <!-- Total Views -->
                    <div class="stats-card rounded-2xl shadow-xl p-6 border border-purple-200">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-purple-600 text-sm font-semibold uppercase tracking-wider mb-1">Total Views</p>
                                <p class="text-4xl font-bold text-purple-700 counter-animation"><%= stats.totalViews.toLocaleString() %></p>
                                <p class="text-purple-500 text-sm mt-1">👁️ Engagement</p>
                            </div>
                            <div class="w-16 h-16 bg-gradient-to-br from-purple-400 to-purple-600 rounded-full flex items-center justify-center shadow-lg">
                                <i data-lucide="eye" class="w-8 h-8 text-white"></i>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Analytics Section -->
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8 mb-8">
                    <!-- Category Breakdown Chart -->
                    <div class="bg-white rounded-2xl shadow-xl p-6 border border-gray-200 lg:col-span-1">
                        <div class="flex items-center justify-between mb-6">
                            <h3 class="text-xl font-bold text-gray-800">Category Distribution</h3>
                            <i data-lucide="pie-chart" class="w-6 h-6 text-blue-500"></i>
                        </div>
                        <div class="space-y-4">
                            <% categoryStats.slice(0, 5).forEach((category, index) => { %>
                                <% 
                                    const colorClasses = ['bg-blue-500', 'bg-emerald-500', 'bg-orange-500', 'bg-purple-500', 'bg-pink-500'];
                                    const colorValues = ['#3b82f6', '#10b981', '#f97316', '#8b5cf6', '#ec4899'];
                                    const colorClass = colorClasses[index % colorClasses.length];
                                    const colorValue = colorValues[index % colorValues.length];
                                    const percentage = ((category.count / stats.totalGemstones) * 100).toFixed(1);
                                %>
                                <div class="space-y-2">
                                    <div class="flex items-center justify-between">
                                        <div class="flex items-center flex-1">
                                            <div class="w-4 h-4 <%= colorClass %> rounded-full mr-3"></div>
                                            <span class="font-medium text-gray-700 text-base"><%= category._id || 'Uncategorized' %></span>
                                        </div>
                                        <div class="flex items-center ml-4">
                                            <span class="text-sm text-gray-500 mr-3"><%= percentage %>%</span>
                                            <span class="font-bold text-gray-800 text-lg"><%= category.count %></span>
                                        </div>
                                    </div>
                                    <div class="w-full bg-gray-200 rounded-full h-2">
                                        <div class="<%= colorClass %> h-2 rounded-full transition-all duration-700 ease-out progress-bar" data-width="<%= percentage %>"></div>
                                    </div>
                                </div>
                            <% }); %>
                            <% if (categoryStats.length > 5) { %>
                                <div class="text-center pt-3 border-t border-gray-100">
                                    <span class="text-sm text-gray-500">+ <%= categoryStats.length - 5 %> more categories</span>
                                </div>
                            <% } %>
                        </div>
                    </div>

                    <!-- Top Performing Gemstones -->
                    <div class="bg-white rounded-2xl shadow-xl p-6 border border-gray-200 lg:col-span-2">
                        <div class="flex items-center justify-between mb-6">
                            <div>
                                <h3 class="text-xl font-bold text-gray-800">Top Performers</h3>
                                <p class="text-sm text-gray-500 mt-1">Most viewed gemstones in your collection</p>
                            </div>
                            <i data-lucide="trophy" class="w-6 h-6 text-golden"></i>
                        </div>
                        <% if (topViewedGemstones && topViewedGemstones.length > 0) { %>
                            <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
                                <% topViewedGemstones.slice(0, 8).forEach((gemstone, index) => { %>
                                    <div class="group top-performer-card p-4 bg-gradient-to-r from-gray-50 to-gray-100 rounded-xl hover:from-blue-50 hover:to-purple-50 hover:shadow-md transition-all duration-300 cursor-pointer min-h-[90px]" onclick="window.location.href='/admin/gemstones/edit/<%= gemstone._id %>'">
                                        <div class="top-performer-content flex items-center">
                                            <div class="relative mr-3 flex-shrink-0">
                                                <div class="w-12 h-12 bg-gradient-to-br from-golden via-yellow-400 to-orange-400 rounded-full flex items-center justify-center text-white font-bold text-lg shadow-lg group-hover:scale-110 transition-transform duration-300">
                                                    <%= index + 1 %>
                                                </div>
                                                <% if (index < 3) { %>
                                                    <div class="absolute -top-1 -right-1 w-4 h-4 bg-gradient-to-r from-yellow-400 to-orange-500 rounded-full flex items-center justify-center">
                                                        <i data-lucide="<%= index === 0 ? 'crown' : 'star' %>" class="w-2 h-2 text-white"></i>
                                                    </div>
                                                <% } %>
                                            </div>
                                            <div class="flex-1 min-w-0">
                                                <p class="font-bold text-gray-900 text-base lg:text-lg truncate group-hover:text-blue-700 transition-colors">
                                                    <%= gemstone.name.english %>
                                                </p>
                                                <p class="text-sm text-gray-600 capitalize truncate">
                                                    <i data-lucide="gem" class="w-3 h-3 inline mr-1"></i>
                                                    <%= gemstone.category %>
                                                </p>
                                            </div>
                                        </div>
                                        <div class="top-performer-views text-right">
                                            <div class="flex items-center justify-end mb-1">
                                                <i data-lucide="eye" class="w-4 h-4 text-blue-500 mr-1"></i>
                                                <p class="font-bold text-xl lg:text-2xl text-blue-600">
                                                    <%= gemstone.viewCount.toLocaleString() %>
                                                </p>
                                            </div>
                                            <p class="text-xs text-gray-500 uppercase tracking-wide">views</p>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                            <% if (topViewedGemstones.length > 8) { %>
                                <div class="text-center pt-6 border-t border-gray-100 mt-6">
                                    <a href="/admin/gemstones" class="inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
                                        <span>View All <%= topViewedGemstones.length %> Gemstones</span>
                                        <i data-lucide="arrow-right" class="w-4 h-4 ml-2"></i>
                                    </a>
                                </div>
                            <% } %>
                        <% } else { %>
                            <div class="text-center py-12 text-gray-500">
                                <div class="w-20 h-20 bg-gradient-to-br from-gray-200 to-gray-300 rounded-full flex items-center justify-center mx-auto mb-4">
                                    <i data-lucide="gem" class="w-10 h-10 text-gray-400"></i>
                                </div>
                                <h4 class="text-lg font-semibold text-gray-700 mb-2">No Performance Data Yet</h4>
                                <p class="text-sm text-gray-500 mb-6 max-w-sm mx-auto">Add some gemstones and wait for visitors to view them to see top performers here.</p>
                                <a href="/admin/gemstones/add" class="inline-flex items-center px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
                                    <i data-lucide="plus" class="w-4 h-4 mr-2"></i>
                                    Add Your First Gemstone
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>


                <!-- Frontend Preview Section -->
                <div class="bg-white rounded-2xl shadow-xl p-6 mb-8 border border-gray-200">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="text-xl font-bold text-gray-800">Frontend Preview</h3>
                        <i data-lucide="monitor" class="w-6 h-6 text-blue-500"></i>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                        <a href="<%= frontendUrl %>/" target="_blank" class="flex items-center p-4 bg-gradient-to-r from-blue-500 to-blue-600 text-white rounded-lg hover:from-blue-600 hover:to-blue-700 transition-all transform hover:scale-105">
                            <i data-lucide="home" class="w-6 h-6 mr-3"></i>
                            <div>
                                <p class="font-semibold">Home Page</p>
                                <p class="text-sm opacity-90">View your website</p>
                            </div>
                        </a>
                        <a href="<%= frontendUrl %>/gemstones" target="_blank" class="flex items-center p-4 bg-gradient-to-r from-emerald-500 to-emerald-600 text-white rounded-lg hover:from-emerald-600 hover:to-emerald-700 transition-all transform hover:scale-105">
                            <i data-lucide="gem" class="w-6 h-6 mr-3"></i>
                            <div>
                                <p class="font-semibold">All Gemstones</p>
                                <p class="text-sm opacity-90">Browse collection</p>
                            </div>
                        </a>
                        <a href="<%= frontendUrl %>/contact" target="_blank" class="flex items-center p-4 bg-gradient-to-r from-orange-500 to-orange-600 text-white rounded-lg hover:from-orange-600 hover:to-orange-700 transition-all transform hover:scale-105">
                            <i data-lucide="phone" class="w-6 h-6 mr-3"></i>
                            <div>
                                <p class="font-semibold">Contact Page</p>
                                <p class="text-sm opacity-90">Customer inquiries</p>
                            </div>
                        </a>
                    </div>
                    <div class="mt-4 p-3 bg-blue-50 rounded-lg">
                        <p class="text-sm text-blue-700">💡 <strong>Tip:</strong> Changes made in the admin dashboard will reflect immediately on the frontend. Use these links to preview your updates!</p>
                    </div>
                </div>

                <!-- Recent Gemstones -->
                <div class="bg-white rounded-2xl shadow-xl p-6 border border-gray-200">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-gray-800">Recently Added</h3>
                        <a href="/admin/gemstones" class="text-blue-600 hover:text-blue-800 text-sm font-medium flex items-center">
                            View All
                            <i data-lucide="arrow-right" class="w-4 h-4 ml-1"></i>
                        </a>
                    </div>
                    
                    <% if (recentGemstones && recentGemstones.length > 0) { %>
                        <!-- Mobile: Stack layout, Desktop: Grid layout -->
                        <div class="space-y-3 lg:space-y-0 lg:grid lg:grid-cols-1 lg:gap-3">
                            <% recentGemstones.slice(0, 6).forEach((gemstone, index) => { %>
                                <div class="recent-gemstone-card flex items-center p-3 lg:p-4 bg-gradient-to-r from-gray-50 to-gray-100 rounded-xl hover:from-blue-50 hover:to-purple-50 hover:shadow-md transition-all duration-300 cursor-pointer group" onclick="window.location.href='/admin/gemstones/edit/<%= gemstone._id %>'">
                                    <div class="flex-shrink-0">
                                        <% if (gemstone.images && gemstone.images.length > 0) { %>
                                            <img src="<%= gemstone.images[0].url %>" alt="<%= gemstone.name.english %>" class="w-12 h-12 lg:w-14 lg:h-14 rounded-xl object-cover border-2 border-white shadow-md group-hover:scale-110 transition-transform duration-300">
                                        <% } else { %>
                                            <div class="w-12 h-12 lg:w-14 lg:h-14 bg-gradient-to-br from-blue-400 to-purple-500 rounded-xl flex items-center justify-center border-2 border-white shadow-md group-hover:scale-110 transition-transform duration-300">
                                                <i data-lucide="gem" class="w-6 h-6 lg:w-7 lg:h-7 text-white"></i>
                                            </div>
                                        <% } %>
                                    </div>
                                    
                                    <div class="ml-4 flex-1 min-w-0">
                                        <div class="flex items-center justify-between">
                                            <div class="flex items-center min-w-0 flex-1">
                                                <p class="font-bold text-gray-900 text-base lg:text-lg truncate group-hover:text-blue-700 transition-colors"><%= gemstone.name.english %></p>
                                                <% if (gemstone.isTrending) { %>
                                                    <span class="ml-2 px-2 py-1 bg-gradient-to-r from-orange-400 to-red-500 text-white text-xs font-bold rounded-full shadow-md flex items-center">
                                                        <i data-lucide="trending-up" class="w-3 h-3 mr-1"></i>
                                                        HOT
                                                    </span>
                                                <% } %>
                                            </div>
                                            <div class="flex-shrink-0 ml-4 text-right hidden sm:block">
                                                <span class="text-xs text-gray-400 font-medium">
                                                    <i data-lucide="calendar" class="w-3 h-3 inline mr-1"></i>
                                                    <%= new Date(gemstone.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %>
                                                </span>
                                            </div>
                                        </div>
                                        
                                        <div class="flex items-center mt-2 flex-wrap gap-2">
                                            <span class="inline-flex items-center px-2 py-1 bg-blue-100 text-blue-800 text-xs font-medium rounded-full">
                                                <i data-lucide="gem" class="w-3 h-3 mr-1"></i>
                                                <%= gemstone.category %>
                                            </span>
                                            <span class="inline-flex items-center px-2 py-1 bg-purple-100 text-purple-800 text-xs font-medium rounded-full">
                                                <i data-lucide="eye" class="w-3 h-3 mr-1"></i>
                                                <%= gemstone.viewCount %> views
                                            </span>
                                            <% if (gemstone.priceRange && gemstone.priceRange.min) { %>
                                                <span class="inline-flex items-center px-2 py-1 bg-green-100 text-green-800 text-xs font-medium rounded-full">
                                                    <i data-lucide="indian-rupee" class="w-3 h-3 mr-1"></i>
                                                    <%= gemstone.priceRange.min.toLocaleString() %>+
                                                </span>
                                            <% } %>
                                            <div class="sm:hidden">
                                                <span class="text-xs text-gray-400">
                                                    <%= new Date(gemstone.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %>
                                                </span>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            <% }); %>
                        </div>
                        
                        <% if (recentGemstones.length > 6) { %>
                            <div class="text-center pt-4 mt-6 border-t border-gray-100">
                                <a href="/admin/gemstones" class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-blue-500 to-blue-600 text-white rounded-xl hover:from-blue-600 hover:to-blue-700 transition-all font-semibold shadow-lg hover:shadow-xl transform hover:scale-105">
                                    <span>View All <%= recentGemstones.length %> Gemstones</span>
                                    <i data-lucide="arrow-right" class="w-4 h-4 ml-2"></i>
                                </a>
                            </div>
                        <% } %>
                    <% } else { %>
                        <div class="text-center py-8">
                            <i data-lucide="plus-circle" class="w-16 h-16 mx-auto text-gray-300 mb-4"></i>
                            <p class="text-gray-500 mb-4">No gemstones added yet</p>
                            <a href="/admin/gemstones/add" class="inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
                                <i data-lucide="plus" class="w-4 h-4 mr-2"></i>
                                Add Your First Gemstone
                            </a>
                        </div>
                    <% } %>
                </div>

                <!-- System Health Monitor -->
                <div class="bg-white rounded-2xl shadow-xl p-6 mb-8 border border-gray-200">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="text-xl font-bold text-gray-800">System Health</h3>
                        <i data-lucide="activity" class="w-6 h-6 text-green-500"></i>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                        <!-- Backend Status -->
                        <div class="flex items-center p-4 bg-green-50 rounded-lg border border-green-200">
                            <div class="w-3 h-3 bg-green-500 rounded-full mr-3 animate-pulse"></div>
                            <div>
                                <p class="font-semibold text-green-800">Backend API</p>
                                <p class="text-sm text-green-600">Online</p>
                            </div>
                        </div>

                        <!-- Frontend Status -->
                        <div class="flex items-center p-4 bg-blue-50 rounded-lg border border-blue-200">
                            <div class="w-3 h-3 bg-blue-500 rounded-full mr-3 animate-pulse"></div>
                            <div>
                                <p class="font-semibold text-blue-800">Frontend</p>
                                <p class="text-sm text-blue-600">Active</p>
                            </div>
                        </div>

                        <!-- Database Status -->
                        <div class="flex items-center p-4 bg-purple-50 rounded-lg border border-purple-200">
                            <div class="w-3 h-3 bg-purple-500 rounded-full mr-3 animate-pulse"></div>
                            <div>
                                <p class="font-semibold text-purple-800">MongoDB</p>
                                <p class="text-sm text-purple-600">Connected</p>
                            </div>
                        </div>

                        <!-- Cloudinary Status -->
                        <div class="flex items-center p-4 bg-orange-50 rounded-lg border border-orange-200">
                            <div class="w-3 h-3 bg-orange-500 rounded-full mr-3 animate-pulse"></div>
                            <div>
                                <p class="font-semibold text-orange-800">Cloudinary</p>
                                <p class="text-sm text-orange-600">Active</p>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <!-- Scripts -->
    <script>
        // Initialize Lucide icons
        lucide.createIcons();
        
        // Mobile menu functionality
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const sidebar = document.getElementById('sidebar');
        const mobileMenuOverlay = document.getElementById('mobile-menu-overlay');
        const closeSidebarButton = document.getElementById('close-sidebar');

        if (mobileMenuButton) {
            mobileMenuButton.addEventListener('click', () => {
                sidebar.classList.remove('-translate-x-full');
                mobileMenuOverlay.classList.remove('hidden');
            });
        }

        const closeMobileMenu = () => {
            sidebar.classList.add('-translate-x-full');
            mobileMenuOverlay.classList.add('hidden');
        };

        if (closeSidebarButton) {
            closeSidebarButton.addEventListener('click', closeMobileMenu);
        }

        if (mobileMenuOverlay) {
            mobileMenuOverlay.addEventListener('click', closeMobileMenu);
        }



        // Counter animation
        function animateCounters() {
            const counters = document.querySelectorAll('.counter-animation');
            
            counters.forEach(counter => {
                const target = parseInt(counter.textContent.replace(/,/g, ''));
                const increment = target / 50;
                let current = 0;
                
                const timer = setInterval(() => {
                    current += increment;
                    if (current >= target) {
                        counter.textContent = target.toLocaleString();
                        clearInterval(timer);
                    } else {
                        counter.textContent = Math.floor(current).toLocaleString();
                    }
                }, 30);
            });
        }

        // Auto-refresh dashboard every 5 minutes
        setInterval(() => {
            fetch('/admin/api/analytics')
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        // Update any real-time data if needed
                        console.log('Dashboard data refreshed');
                    }
                })
                .catch(error => console.error('Auto-refresh failed:', error));
        }, 300000); // 5 minutes

        // Initialize animations on load
        document.addEventListener('DOMContentLoaded', () => {
            animateCounters();
            
            // Initialize progress bars
            const progressBars = document.querySelectorAll('.progress-bar');
            progressBars.forEach(bar => {
                const width = bar.getAttribute('data-width');
                if (width) {
                    // Add a small delay for animation
                    setTimeout(() => {
                        bar.style.width = width + '%';
                    }, 100);
                }
            });
            
            // Add hover effects to stats cards
            const statsCards = document.querySelectorAll('.stats-card');
            statsCards.forEach(card => {
                card.addEventListener('mouseenter', () => {
                    card.style.transform = 'translateY(-8px) scale(1.02)';
                });
                
                card.addEventListener('mouseleave', () => {
                    card.style.transform = 'translateY(0) scale(1)';
                });
            });
        });

        // Notification system
        function showNotification(message, type = 'success') {
            const notification = document.createElement('div');
            notification.className = `fixed top-4 right-4 z-50 p-4 rounded-lg shadow-lg transition-all duration-300 ${
                type === 'success' ? 'bg-green-500 text-white' : 'bg-red-500 text-white'
            }`;
            notification.innerHTML = `
                <div class="flex items-center">
                    <i data-lucide="${type === 'success' ? 'check-circle' : 'alert-circle'}" class="w-5 h-5 mr-2"></i>
                    <span>${message}</span>
                </div>
            `;
            
            document.body.appendChild(notification);
            lucide.createIcons();
            
            // Auto remove after 3 seconds
            setTimeout(() => {
                notification.style.opacity = '0';
                notification.style.transform = 'translateX(100%)';
                setTimeout(() => {
                    document.body.removeChild(notification);
                }, 300);
            }, 3000);
        }

        // Quick action confirmation
        function confirmAction(message, callback) {
            if (confirm(message)) {
                callback();
            }
        }
    </script>
    
    <script src="/admin/assets/js/admin.js"></script>
</body>
</html> 